{
  "name": "postie-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Postie Worker — AI Facebook post generator & link creator",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute postie-db --file=./schema.sql",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "hono": "^4.12.30"
  }
}
